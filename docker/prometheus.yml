# Prometheus scrape config for the Compose stack. Prometheus reaches the app directly on the
# internal network (app:8080), not through the nginx edge.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: mini-wsa
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ['app:8080']
